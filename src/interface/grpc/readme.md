# gRPC Client interface节点

## gRPC依赖安装
``` bash
sudo apt update
sudo apt install -y \
    build-essential \
    cmake \
    pkg-config \
    libgrpc++-dev \
    libprotobuf-dev \
    protobuf-compiler-grpc
```

## Client端编译与测试
* /src/